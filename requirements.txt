# Core
Django==5.1.3
gunicorn==23.0.0
psycopg[binary]==3.2.3
python-decouple==3.8

# Security / Auth
argon2-cffi==23.1.0
django-axes==7.0.0

# Static files
whitenoise==6.8.2

# Entra OIDC (Phase 2 — installiert, aber konfigurativ deaktiviert im MVP)
mozilla-django-oidc==4.0.1

# UI helpers (optional, klein)
django-htmx==1.21.0

# Dev / test (optional, nicht in Produktion zwingend)
# pytest-django==4.9.0
